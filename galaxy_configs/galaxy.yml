gravity:
  gunicorn:
    enable: true
    bind: 0.0.0.0:8080
  celery:
  handlers:
    handler:
      processes: 2
      pools:
        - job-handlers
  gx_it_proxy:
  tusd:
  reports:
galaxy:
  admin_users: dima@local.hpc
  galaxy_infrastructure_url: http://192.168.2.2:8080
  job_config_file: config/job_conf.yml

  default_job_external_setup: true
