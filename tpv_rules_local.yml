---
global:
  default_inherits: default

destinations:
  local_dest:
    runner: local
    tags:
      - local_dest
  remote_pulsar:
    runner: pulsar_mq
    tags:
      - remote_pulsar

tools:
  default:
    scheduling:
      require:
        - local_dest

  ChangeCase:
    scheduling:
      require:
        - remote_pulsar
